// import { StringMask } from 'string-mask';

// const phoneMask = '(000) 000-0000';

// export const formatPhoneNumber = (phone: string | null): string | null => {
//   if (!phone) return null;
  
//   const formatter = new StringMask(phoneMask);
//   return formatter.validate(phone) ? formatter.apply(phone) : null;
// };

// export const isPhoneNumber = (phone: string | null): boolean => {
//   if (!phone) return false;
//   return /^\(\d{3}\) \d{3}-\d{4}$/.test(phone);
// };
